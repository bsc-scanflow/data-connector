# Scanflow requirements - According to 'ncloud2' environment

# Due to recent changes in Docker SDK, we should bump the following packages' version at some point
# - 'http+docker' Schema bug: https://github.com/docker/docker-py/pull/3257#issuecomment-2173911243
# - docker==7.1.0
# - mlflow==2.16.2
# - requests==2.32+ --> This is already a docker dependency
# But couler-scanflow only accepts docker version 4.1.0, so we can't do it
# Trying to pin a valid requests version prior to other changes
requests==2.31.0
couler-scanflow==0.1.2
docker==4.1.0
mlflow==2.9.2

matplotlib==3.8.2

click==8.1.7
fastapi==0.108.0
uvicorn==0.25.0

kubernetes==28.1.0
boto3==1.34.15

# Workarounds for broken cython compatibility
#pyyaml!=6.0.0,!=5.4.0,!=5.4.1 # pyyaml is broken with cython 3
pyaml==23.12.0

# Missing dependencies
aiohttp


# Scanflow requirements not found in 'ncloud2' environment
# - seldon-core arises dependency conflicts with the rest of the packages
#seldon-core==1.8.0